Put your bundled 7zz binary in this folder with exact name:

7zz

At runtime the app will try bundled binary first (if enabled in UI), then fallback to user path/system paths.

Recommended permission before packaging:
chmod +x 7zz
